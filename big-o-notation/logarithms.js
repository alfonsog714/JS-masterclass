/* 
    What is a logarithm?
        - Essentially the inverse operation of an exponent.
        - Example: log2(8) = 3 because 2^3 = 8.
        - log2(value) = exponent ==> 2^exponent = value
        - In big o notation, the subscript or base for log is always 2.
*/